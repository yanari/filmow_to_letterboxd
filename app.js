const { parsePage } = require('./parser');

// getLastPage('weltanschauung', (title, directors, year, rating, page) => {
//     console.log(title, directors, year, rating, 'page:', page);
// });

parsePage('weltanschauung');